export function saveFormData(data) {
    // Realiza cualquier operación necesaria con los datos, por ejemplo, enviarlos a un servidor
    // En este caso, simplemente los mostramos en la consola
    console.log("Datos recibidos:", data);
}
